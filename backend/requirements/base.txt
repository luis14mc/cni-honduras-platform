# Core dependencies shared by dev and production.
# Pin upper bounds loosely; align with docker/backend/Dockerfile (Python 3.11+).
Django>=5.1,<5.3
djangorestframework>=3.15,<3.16
django-environ>=0.11.2
psycopg2-binary>=2.9.9
django-cors-headers>=4.3.1
